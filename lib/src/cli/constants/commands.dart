const String addCommand = 'add';

const String configCommand = 'config';

const String dartCommand = 'dart';

const String doctorCommand = 'doctor';

const String globalCommand = 'global';

const String installCommand = 'install';

const String listCommand = 'list';

const String localCommand = 'local';

const String removeCommand = 'remove';
